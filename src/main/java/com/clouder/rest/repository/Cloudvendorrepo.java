package com.clouder.rest.repository;

import com.clouder.rest.model.vendors;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Cloudvendorrepo extends JpaRepository<vendors, String> {
}
